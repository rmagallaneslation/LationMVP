# Deployment Note

This repository deploys through Vercel. GitHub Actions should be used for checks only unless the team explicitly adds a Vercel deployment workflow later.

Cloudflare Pages, n8n, Docker server deployment, and standalone backend deployment are not part of this landing page architecture.

Required runtime configuration is documented in `README.md` and `DEPLOYMENT.md`.
